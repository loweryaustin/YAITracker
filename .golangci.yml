version: "2"

linters:
  exclusions:
    rules:
      # MCP tool handlers intentionally return (errResult, nil): the Go error
      # is reserved for transport failures while business errors are conveyed
      # via errResult (IsError=true on the MCP CallToolResult).
      - path: internal/mcp/tools\.go
        linters:
          - nilerr
